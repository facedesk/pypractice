coins = [
[0.25,0],
[0.1,0],
[0.05,0],
[0.01,0]
]
def sum(coins):
	sum = 0
	for coin in coins:
		sum += coin[0]*coin[1]
	return sum

def canAdd(coins,index, coin):
	target = sum(coins) + coin[1]
	if(target ==1):
		return False
	while(

	




def generate(coins):
	for c1 in coins:
		for c2 in coins:
			if(sum(coins)!= 1):
				c1[1]+=1

print(generate(coins))
